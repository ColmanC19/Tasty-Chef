
<h1>Add a new tag:</h1>

<%= form_for [@recipe, @tag] do |f| %>
  <%= f.label :name %>
  <%= f.collection_select :name %>
  <%= f.label :ingredient %>
  <%= f.check_box :ingredient %>
  <p><%= f.submit %></p>
<% end %>
